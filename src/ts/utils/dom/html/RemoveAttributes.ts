/**
 * remove a given attribute from all nodes in an array
 * @param nodes
 * @param attribute
 */
export default (nodes: Node[], attribute: string) : Node[] =>
{
    return null;
    
    //  filter out any nodes that lack the relevant attribute

    // nodes = filter(nodes, (node: HTMLElement) =>
    //     {
    //         return node.getAttribute && node.getAttribute(attribute)
    //     });
    //
    // //  then for each remaining node,
    // //  remove its attribute
    //
    // forEach(nodes, (node: HTMLElement) =>
    //     {
    //         node.removeAttribute(attribute);
    //     });
    //
    // //  and return the list of affected nodes
    //
    // return nodes;
}